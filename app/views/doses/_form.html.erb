<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient, input_html: { name: 'dose[ingredient_id]' }, collection: @ingredients %>
  <%= f.submit %>
<% end %>
